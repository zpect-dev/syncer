services:
    syncer:
        build: .
        container_name: profit_syncer
        command: /app/syncer
        restart: unless-stopped
        env_file:
            - .env
        environment:
            - POSTGRES_URL=postgres://postgres:secret@${TAILSCALE_IP}:5432/profit_ecommerce?sslmode=disable